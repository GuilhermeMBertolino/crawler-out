#!/bin/sh
# check if we are on real system
if [ -z "${IPKG_INSTROOT}" ]; then
    mkdir -p /usr/bin/avira/libauc/libauc_1.0.0-1
    mkdir -p /usr/include/avira/auc
    mkdir -p /usr/lib/avira
fi
exit 0
