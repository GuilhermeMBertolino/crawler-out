echo "[pega-defaults] patch_db-1 running ..." > /dev/console

# add section db for /etc/config/netgear
uci set netgear.db=data

# add 'exit 0' in the end of file (all commands MUST be before 'exit 0')
exit 0

