#!/usr/bin/lua

local json = require "luci.json"
local portForward = require "webGetFunc.portForward"

local log = require("luci.log") --for debug
local portTriggerTable = portForward.getPortTriggerList()

local data = { }
data = portTriggerTable

return json.encode(data)
