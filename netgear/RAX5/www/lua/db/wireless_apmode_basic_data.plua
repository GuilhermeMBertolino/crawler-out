#!/usr/bin/lua

local json = require "luci.json"
local commonCfg = require "webGetFunc.commonCfg"

local data = { }
data["mode"] = { value = commonCfg.getOperationMode(), type = 'radio' }

return json.encode(data)

