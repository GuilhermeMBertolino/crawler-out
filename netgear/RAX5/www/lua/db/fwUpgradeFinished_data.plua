#!/usr/bin/lua

local json = require "luci.json"
local wizard  = require "webGetFunc.wizard"
local common  = require "webGetFunc.commonCfg"

local data = { }

data["upgrade_finished"] = { value = wizard.getWizardFwUpgradeInProgress()}
data["new_csrf_token"] = { value =  common.genCsrfTokenFile("pureToken")}

return json.encode(data)