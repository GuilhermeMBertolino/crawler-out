#!/usr/bin/lua

local json = require "luci.json"
local portForward = require "webGetFunc.portForward"
local portForwardTable = portForward.getPortForwardList()

local data = { }
data = portForwardTable

return json.encode(data)