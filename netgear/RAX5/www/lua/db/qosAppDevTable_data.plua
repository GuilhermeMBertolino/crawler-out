#!/usr/bin/lua

local json = require "luci.json"
local attachedDev  = require "webGetFunc.attachedDev"
local log = require("luci.log") --for debug

local qosAppDevTable = attachedDev.getQosAppDevValue()

local data = { }

data = qosAppDevTable

return json.encode(data)

