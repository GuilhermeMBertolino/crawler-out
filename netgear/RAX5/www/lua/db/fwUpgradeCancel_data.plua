#!/usr/bin/lua

local json = require "luci.json"
local fw = require "commonFunc.fwUtils"

local data = { }

data["upgrade_status"] = { value =  fw.getFwUpgradeCancel()}

return json.encode(data)
