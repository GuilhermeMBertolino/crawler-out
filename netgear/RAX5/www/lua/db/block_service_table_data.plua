#!/usr/bin/lua

local json = require "luci.json"
local blockServices  = require "webGetFunc.blockServices"
local log = require("luci.log") --for debug

local blockServicesTable = blockServices.getBlockServicesTableValue()

local data = { }

data = blockServicesTable

return json.encode(data)

