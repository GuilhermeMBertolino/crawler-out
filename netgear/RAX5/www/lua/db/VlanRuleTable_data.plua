#!/usr/bin/lua

local json = require "luci.json"
local vlan  = require "webGetFunc.vlan"
local log = require("luci.log") --for debug

local vlan_rule_table = vlan.getVlanRuleTable()

local data = { }

data = vlan_rule_table

return json.encode(data)

