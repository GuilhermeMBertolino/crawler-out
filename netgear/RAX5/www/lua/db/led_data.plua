#!/usr/bin/lua
local json = require "luci.json"
local led = require "webGetFunc.led"
local log = require("luci.log") --for debug
local data = { }
data["enableLed"] = { value = led.getRadioVal_enableLed(), type = "radio"}

return json.encode(data)