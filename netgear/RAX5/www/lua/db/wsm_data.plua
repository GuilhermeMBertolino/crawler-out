#!/usr/bin/lua

local json = require "luci.json"

local log = require "luci.log"

local wsmCfg = require "webGetFunc.wsmCfg"

local enableWsm= wsmCfg.getEanbleWsm()

local data = {}

data["enableWsm"] = { value = enableWsm, type = "checkbox" }

--io.write("Content-type: %s" %  {"application/json"} )
--io.write("\r\n\r\n")
--io.write(json.encode(data))
return json.encode(data)

