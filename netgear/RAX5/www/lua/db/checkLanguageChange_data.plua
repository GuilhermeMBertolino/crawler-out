#!/usr/bin/lua
local language  = require "commonFunc.language"
local json = require "luci.json"

local data = { }

data["changeLangResult"] = { value = language.getChangeLanguageResult()}


return json.encode(data)
