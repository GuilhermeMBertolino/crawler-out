#!/usr/bin/lua

local json = require "luci.json"
local fw = require "commonFunc.fwUtils"
local upgradedResult = {}

upgradedResult["FwUpgradeStatus"] = "Not Ready";
upgradedResult = fw.getFwUpgradeResult()

local data = { }

data["upgrade_status"] = { value = upgradeResult["FwUpgradeStatus"]}

return json.encode(data)

