#!/usr/bin/lua

local json = require "luci.json"

local statistics  = require "webGetFunc.statistics"
local data = { }

data["sysUptime"] = { value = statistics.getSysUpTime(), type = "spantext" }

return json.encode(data)

