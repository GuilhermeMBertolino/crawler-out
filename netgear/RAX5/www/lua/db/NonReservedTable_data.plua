#!/usr/bin/lua

local json = require "luci.json"
local reservedClient  = require "webGetFunc.reservedClient"
local log = require("luci.log") --for debug

local nonReservedTable = reservedClient.getNonReservedTable()

local data = { }

data = nonReservedTable

return json.encode(data)

