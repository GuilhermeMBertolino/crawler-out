#!/usr/bin/lua

local json = require "luci.json"
local interface  = require "webGetFunc.interface"

local data = { }

data["internetStatus"] = { value = interface.getWizardInternetStatus()}

return json.encode(data)

