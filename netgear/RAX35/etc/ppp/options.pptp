noipdefault
noauth
nobsdcomp
nodeflate
#20181016, For the idle timeout and dial on demand, comment this "idle 0" to avoid idle timeout(connection down) working abnormally.
#idle 0
#20181122, To support the PPTP server which using CHAP as authentication protocol and without using MPPE.
#mppe required,no40,no56,stateless
maxfail 0
